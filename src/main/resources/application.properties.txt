# ---------------------------
# Configuraci?n de la base de datos
# ---------------------------

# URL de conexi?n a la base de datos MySQL
spring.datasource.url=jdbc:mysql://172.16.1.64:3306/veterinaria_C1

# Usuario de la base de datos
spring.datasource.username=bdd1

# Contrase?a de la base de datos
spring.datasource.password=bdd1

# Driver JDBC que se usa para conectarse a MySQL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ---------------------------
# Configuraci?n de Hibernate / JPA
# ---------------------------

# Indica qu? debe hacer Hibernate con las tablas:
# Opciones:
# - none: No hace nada.
# - validate: Verifica que las tablas existan y coincidan con las entidades (no crea ni actualiza).
# - update: Crea tablas nuevas o modifica las existentes sin borrar datos.
# - create: Borra todo y crea las tablas de cero al arrancar (?cuidado! se pierden datos).
# - create-drop: Crea las tablas al iniciar y las borra al apagar la app.
spring.jpa.hibernate.ddl-auto=update

# Muestra en consola las consultas SQL que ejecuta Hibernate (muy ?til para debug)
spring.jpa.show-sql=true

# Dialecto de base de datos: le indica a Hibernate c?mo generar las consultas SQL espec?ficas para MySQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
